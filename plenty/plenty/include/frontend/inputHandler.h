#pragma once

#include <string>
#include <unordered_map>

class InputHandler
{
private:
	bool isRunning;
	bool isPriv;

// command functions
private:
	void ping()


private:
	std::unordered_map<std::string, >
public:
	InputHandler();
	
	void handleInput();

	operator bool()
	{
		return isRunning;
	}
};